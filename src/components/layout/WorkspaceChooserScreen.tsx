"use client";
import { useRouter } from "next/navigation";
import { useWorkspace } from "@/contexts/WorkspaceContext";
import { useAuth } from "@/contexts/AuthContext";
export default function WorkspaceChooserScreen(){const{organizations,memberships,setActiveOrgId}=useWorkspace();const{user}=useAuth();const router=useRouter();return <div className="flex min-h-screen items-center justify-center bg-slate-950 px-6 py-12 text-slate-100"><div className="w-full max-w-xl rounded-xl border border-slate-800 bg-slate-900 p-8 shadow-xl"><h1 className="text-lg font-semibold">Choose your Translend workspace</h1><p className="mt-1 text-sm text-slate-400">{user?.email?`Signed in as ${user.email}. `:""}Select the company you want to work in. You can switch later.</p><div className="mt-6 space-y-3">{organizations.map(org=>{const membership=memberships.find(m=>m.orgId===org.id);return <button key={org.id} type="button" onClick={()=>{setActiveOrgId(org.id);router.replace(`/${org.id}/control-tower`)}} className="w-full rounded-lg border border-slate-700 bg-slate-950 p-4 text-left hover:border-sky-600"><div className="flex items-center justify-between gap-4"><strong>{org.name}</strong><span className="rounded-full bg-sky-950/50 px-2 py-0.5 text-xs text-sky-300">{membership?.role??"member"}</span></div><div className="mt-1 text-xs text-slate-500">{org.country} · {org.currency}</div></button>})}</div></div></div>}
