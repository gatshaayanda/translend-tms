export default function OfflinePage(){return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:24}}><section className="panel" style={{maxWidth:520,textAlign:"center"}}><h1 className="page-title">You are offline</h1><p className="page-subtitle">Translend is waiting for a connection. Previously loaded app screens remain available where your browser has cached them; live writes and fresh data will resume when you reconnect.</p></section></main>;}